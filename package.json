{
  "name": "done-todo-list",
  "version": "1.0.0",
  "private": true,
  "description": "A responsive, browser-local to-do application",
  "scripts": { "start": "node server.cjs", "check": "node --check dist/app.js && node --check server.cjs" },
  "engines": { "node": ">=18" }
}
